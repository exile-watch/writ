{
  "name": "writ",
  "version": "0.0.0",
  "description": "Design system for exile.watch",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/exile-watch/writ.git"
  },
  "author": "Sebastian Krzyzanowski",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/exile-watch/writ/issues"
  },
  "homepage": "https://github.com/exile-watch/writ#readme",
  "scripts": {
    "postinstall": "npx lefthook install && lerna run build",
    "build": "lerna run build",
    "typecheck": "lerna run typecheck",
    "ci:typecheck": "lerna run ci:typecheck",
    "ci:lint": "lerna run ci:lint",
    "lint": "lerna run lint",
    "lint:apply": "lerna run lint:apply",
    "format": "lerna run format",
    "test": "exit 0",
    "cq": "lerna run cq"
  },
  "devDependencies": {
    "@exile-watch/biome-config": "^1.1.0",
    "@exile-watch/conventional-changelog-config": "^1.1.0",
    "@exile-watch/lefthook-config": "^1.1.0",
    "@exile-watch/typescript-config": "^1.1.0",
    "lerna": "^8.1.2"
  },
  "workspaces": ["packages/*"]
}
